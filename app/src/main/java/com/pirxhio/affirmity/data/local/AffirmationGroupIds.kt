package com.pirxhio.affirmity.data.local

/**
 * The implicit group every existing/newly-created affirmation belongs to. Lives in `data/local`
 * (not `ui/groups`) so the data layer never depends on the presentation layer (design D9).
 */
const val PERSONALIZADAS_GROUP_ID = "personalizadas"
