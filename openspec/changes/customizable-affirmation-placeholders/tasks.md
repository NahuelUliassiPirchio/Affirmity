# Tasks: Customizable Affirmation Placeholders

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~900–1100 |
| 400-line budget risk | High |
| Chained PRs recommended | No |
| Suggested split | Single PR (`size:exception`) |
| Delivery strategy | single-pr |
| Chain strategy | size-exception |

Decision needed before apply: Yes
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: High

User locked `single-pr` delivery (Proposal Risks table); `sdd-apply` must record `size:exception`
before starting rather than splitting into chained/stacked PRs. First Room `TypeConverter` +
new pure parser + shared Compose editing primitive genuinely cross three layers in one slice —
honest size, deliberately not split.

### Suggested Work Units

| Unit | Goal | Scope | Verification |
|------|------|-------|---------------|
| 1 | Single PR, `size:exception` | All phases below | `gradlew.bat testDebugUnitTest` + `assembleDebug` + manual Firestore check (Phase 6) |

## Phase 1: Pure Template Layer (TDD)

- [x] 1.1 RED `app/src/test/.../data/AffirmationTemplateTest.kt`: alternating segments in source order; no-bracket text → single `Literal`, byte-identical `render(emptyMap())`; unpaired `[`/`]` stays literal; `[a[b]` → literal `[a` + token `b`; `[]` demoted to literal; verbatim (untrimmed) content; key format `field:ordinal:content`; repeated content gets distinct ordinals; title/subtitle keys never collide.
- [x] 1.2 GREEN `data/AffirmationTemplate.kt`: `TemplateField`, `TemplateSegment`, `AffirmationTemplate`, `AffirmationTemplateParser.parse`/`tokenKey` (Regex `\[([^\[\]]*)\]`, D3).
- [x] 1.3 RED: resolution — override wins when non-blank; blank override loses to original; unknown key ignored, never throws; `render(emptyMap())` == original values.
- [x] 1.4 GREEN: `AffirmationTemplate.valueOf`/`render`.
- [x] 1.5 RED: prune — `[10k]`→`[20k]` drops `title:0:10k`; surrounding-text-only edit keeps the key; inserting a token before shifts ordinals and drops downstream keys.
- [x] 1.6 GREEN: `AffirmationTemplateParser.pruneOverrides`.
- [x] 1.7 RED: `normalizeOverrideValue` — blank/whitespace → `null`; value > `MAX_OVERRIDE_VALUE_LENGTH` (120) rejected/truncated per design.
- [x] 1.8 GREEN: `normalizeOverrideValue` + `MAX_OVERRIDE_VALUE_LENGTH` constant.
- [x] 1.9 REFACTOR: extract shared regex/segment-building helper if 1.2/1.4/1.6 duplicate logic. (Not needed — parse/pruneOverrides/normalizeOverrideValue share the single `TOKEN_REGEX`/`parse` already; no duplicated logic found to extract.)

## Phase 2: Room Persistence (TDD)

- [x] 2.1 RED `app/src/test/.../data/local/OverridesConvertersTest.kt`: round-trip; deterministic sorted-key output regardless of map insertion order; `"`, `\`, `{`, `:` in keys/values survive; `null`/`""`/`"garbage"` → `emptyMap()`; blank values filtered.
- [x] 2.2 GREEN `data/local/OverridesConverters.kt` (first `@TypeConverter`, D7, `org.json`-based).
- [x] 2.3 Modify `data/local/AffirmationEntity.kt`: add `overrides: Map<String, String> = emptyMap()` with `@ColumnInfo(defaultValue = "{}")`.
- [x] 2.4 Modify `data/local/AffirmityDatabase.kt`: `version = 7`, `@TypeConverters(OverridesConverters::class)`, `MIGRATION_6_7` (additive `ALTER TABLE`), append to `addMigrations(...)`.
- [x] 2.5 Modify `data/local/AffirmationDao.kt`: add `updateOverrides(id, overrides)` (`UPDATE ... SET overrides = :overrides WHERE id = :id`, D6).
- [x] 2.6 Generate/commit `app/schemas/.../7.json` (Room `exportSchema`). Auto-generated by KSP via `assembleDebug`/`testDebugUnitTest`; verified the `overrides TEXT NOT NULL DEFAULT '{}'` column matches `MIGRATION_6_7` exactly.
- [x] 2.7 Modify `app/src/androidTest/.../AffirmityDatabaseMigrationTest.kt`: `MIGRATION_6_7` — existing rows survive, `overrides` backfills to `'{}'`, no other column altered. **Test written but NOT run** — `connectedDebugAndroidTest` requires a device/emulator, unavailable in this environment (see 6.3).

## Phase 3: Repository + Firestore Mapping (TDD)

- [x] 3.1 Modify `data/repository/Repositories.kt`: add `AffirmationRepository.setOverrides(id, overrides)` (whole-map replacement contract).
- [x] 3.2 Modify `data/repository/RoomAffirmationRepository.kt`: delegate `setOverrides` → `dao.updateOverrides(id, overrides.filterValues { it.isNotBlank() })`.
- [x] 3.3 RED `app/src/test/.../data/remote/FirestoreMappersTest.kt` additions: `affirmationToMap` includes `overrides`, blanks never emitted; `affirmationFromMap` on a legacy doc without `overrides` → `emptyMap()`; non-string map values ignored; `overridesWritePayload(emptyMap())` yields the field present-and-empty, never absent.
- [x] 3.4 GREEN `data/remote/FirestoreMappers.kt`: `FIELD_OVERRIDES`, `AFFIRMATION_FIELDS`, `sanitizedOverrides`, `overridesWritePayload`; carry `overrides` through `affirmationToMap`/`affirmationFromMap`.
- [x] 3.5 Modify `data/remote/FirestoreAffirmationRepository.kt`: `setOverrides` via `set(overridesWritePayload(...), SetOptions.mergeFields(FIELD_OVERRIDES))` (D4); `insert` switches to `SetOptions.mergeFields(*AFFIRMATION_FIELDS)` (D5).
- [x] 3.6 RED/GREEN `app/src/test/.../data/remote/MigrationPlanTest.kt` + `data/remote/MigrationPlan.kt`/`FirestoreMigrator.kt`: an affirmation with overrides produces a `DocWrite` carrying them; `commitChunk` keeps plain `SetOptions.merge()` (one-time migration path only, no resurrection risk). (`MigrationPlan.kt`/`FirestoreMigrator.kt` needed NO code change — `MigrationPlan.build` already calls `affirmationToMap`, which now carries `overrides` automatically; only the test was added.)

## Phase 4: App State Wiring

- [x] 4.1 Modify `data/AffirmityAppState.kt`: add `Affirmation.overrides`; carry through `toEntity()`/`toAffirmation()`; add `setTokenOverride(affirmationId, tokenKey, rawValue)` — no entitlement guard (D13), no analytics event (D14); calls `pruneOverrides` before `setOverrides`.
- [x] 4.2 Apply `pruneOverrides` on the existing user-affirmation text-edit path (locked decision #7). **DEVIATION**: no user-affirmation text-edit path exists anywhere in the codebase today (`AffirmityAppState` only has add/remove for affirmations; `MyAffirmationsScreen.AffirmationRow` has no edit UI, only delete). This task is a no-op until such a path is built — nothing to wire `pruneOverrides` into. Flagging so design/spec authors know the "locked decision #7" text-edit trigger is presently only reachable via `setTokenOverride`'s own `pruneOverrides` call (4.1), not via a full affirmation-text editor.
- [x] 4.3 Modify `data/AffirmationImport.kt`: KDoc documenting `[token]` bracket syntax for authors (no shape change to `ParsedAffirmation`).

## Phase 5: Compose UI Wiring

- [x] 5.1 Create `ui/affirmations/TokenizedAffirmationText.kt`: `AnnotatedString` render (literal plain, token via `LinkAnnotation.Clickable` + `tokenStyle`), single-active-editor state machine (Idle → Editing → commit on blur/IME-done/back/other-token-tap, D9–D11), `InlineTextContent`-hosted `BasicTextField`.
- [x] 5.2 Modify `ui/affirmations/AffirmationsScreen.kt`: `AffirmationCard` renders title/subtitle via `TokenizedAffirmationText`; add `Modifier.imePadding()`; thread `onOverrideCommitted` param.
- [x] 5.3 Modify `ui/myaffirmations/MyAffirmationsScreen.kt`: `AffirmationRow` uses `TokenizedAffirmationText(editable = true)` (D12); add a `[token]` example to `AFFIRMATIONS_JSON_EXAMPLE`.
- [x] 5.4 Modify `MainActivity.kt`: wire `appState::setTokenOverride` into both screens' `onOverrideCommitted`.

## Phase 6: Verification

- [x] 6.1 Run `gradlew.bat testDebugUnitTest` — full suite green (Phases 1–3 tests + no pre-existing regression). 412 tests, all green, zero failures.
- [x] 6.2 Run `gradlew.bat assembleDebug` — build succeeds.
- [ ] 6.3 Run `gradlew.bat connectedDebugAndroidTest` — `MIGRATION_6_7` coverage from 2.7 green (requires device/emulator). **BLOCKED — no device/emulator available in this environment.** Test code is written and ready (`AffirmityDatabaseMigrationTest.migrate6To7_addsOverridesColumnAndBackfillsExistingRowsToEmptyMap`); needs to be run manually before merge.
- [ ] 6.4 **MANUAL/EMULATOR — cannot be covered by `testDebugUnitTest`.** Confirm `SetOptions.mergeFields(FIELD_OVERRIDES)` performs whole-field replacement, not deep-merge: set an override on device/session A, delete it, confirm device/session B's snapshot listener reports an `overrides` map without the stale key (Firestore emulator or two signed-in sessions). This is the load-bearing assumption of D4 — if it deep-merges instead, fall back to the two-step write noted in design's Open Questions. **BLOCKED — no emulator/device available in this environment; NOT verified. Must be run manually before merge; this is the single highest-risk assumption in the design.**

## Phase 7: Cleanup

- [ ] 7.1 Manual visual pass on `TOKEN_FIELD_EM_PER_CHAR` sizing (design Open Question) — tune the constant if the inline field reads too wide/narrow on device. **BLOCKED — requires a physical/emulator device to visually inspect; not doable in this environment.**
- [x] 7.2 Confirm rollback path: `overrides` column stays additive with `'{}'` default; no `7 → 6` migration is shipped (documented, accepted risk per design). Confirmed: `MIGRATION_6_7` in `AffirmityDatabase.kt` is additive-only; only `addMigrations(MIGRATION_1_2, ..., MIGRATION_6_7)` was appended, no downgrade migration exists.
